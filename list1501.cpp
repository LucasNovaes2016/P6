#include <iostream>
#include <map>
#include <string>

int main(){
	std::map<std::string, int> counts{};
	std::string word{};

	// Read words from the stnadard input and count the number of times
	// each word occurs.
	while (std::cin >> word) 
		++count[word];

	// TODO: Print the results
}